container vs virtual machine

why docker over bare metal and virtual machine?

the images are stateless and donot store any data if added afterwards 
1. ephemeral 
2. dependencies - should be built in the image itself
3. once closed only the image remains

how to persist the data?
1. use docker volumes (recommended)
2. bind map
we are giving location and mounting 